#!/usr/bin/env node

/**
 * @file Run express.js
 * @author Kelompok 5
 */

const app = require("../app")
const port = process.env.PORT || 8000;

app.listen(port, () => {
  console.log(`Listening on http://localhost:${port}`)
})
